services:
  iptv-server:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "10086:10086"
    volumes:
      - ./data:/app/data
      - ./app:/app/app
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped